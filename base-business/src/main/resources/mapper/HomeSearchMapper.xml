<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="icu.liuwisdom.business.mapper.HomeSearchMapper">

    <select id="page" resultType="icu.liuwisdom.business.entity.HomeSearch">
        select id, title, content, images, create_time, type from home_search
        <where>
            <if test="title!=null and title!=''">
                and title like concat('%',#{title},'%')
            </if>
            <if test="content!=null and content!=''">
                and content like concat('%',#{content},'%')
            </if>
            <if test="type!=null and type!=''">
                and type=#{type}
            </if>
            <if test="keyword!=null and keyword!=''">
                and (title like concat('%',#{keyword},'%') or content like concat('%',#{keyword},'%') or
                type=#{keyword})
            </if>
        </where>
    </select>
</mapper>
